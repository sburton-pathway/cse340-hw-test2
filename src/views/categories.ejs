<%- include('partials/header') %>
    <main>
        <h1><%= title %></h1>

        <h2>Popular Categories</h2>
        <ul>
            <% categories.forEach((category) => { %>
                <li><%= category.name %></li>
            <% }) %>
        </ul> 
    </main>   

<%- include('partials/footer') %>